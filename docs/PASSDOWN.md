---
tags: [handoff]
---
# PASSDOWN — Tower Brawl (read this first in a new session)

Godot 4.7 web game (4-player LAN brawler), custom WebSocket relay in Python, browser + phone clients. Repo `~/Work/tower_brawl`, this folder is symlinked into the Obsidian vault as `Tower Brawl`.

## Current status (2026-09-03)
- **Tagged `v0.0.23`** on `master`, working tree clean at handoff. **Phase 3b step 5 (the family playtest) is done**, see [[Playtest v0.0.17]]. Three small fix builds followed it (v0.0.18, v0.0.19, v0.0.20, all below), then **Phase 3c step 0 (v0.0.21, the server dashboard)**, **v0.0.22 (the live operations deck)** and **v0.0.23 (the harness gate and the spectator test ticker)** are done. **Next: Phase 3c proper**, the 5-second history ring and kill-frame stamping (the groundwork for the v0.1.0 replay). Scope it with the user first: what a history frame holds, where the ring lives (client, server or both), how a kill stamps a frame, and how the harness checks it.
- **v0.0.23 "Harness Gate and Spectator Overlay":** while `tools/chaos_bots.py` runs, the server keeps every seat for the bots. `serve_game.py` reads `harness_state.json` once a second (`_harness_tick` in `status_loop`, also at startup): gate closed when `state` is `running` / `restarting`, `written_at` is under 5 s old and the harness `pid` is alive. A human `request_join` (no `bot` field) gets `join_locked`; humans already seated are moved back to spectator through `_release_seat` (the old `leave_slot` body, now shared) and get `join_locked` with `demoted: true`. Plain headless clients send `"bot": "headless"` so `fleet` and `lag` keep working. New server-only packet `harness_status` (`active, state, scenario, done, total, passed, failed, minor`), sent only on change and to new sockets while locked; `status.json` has the same dict under `harness`, the deck header says `SEATS LOCKED (harness)`. Client: `scripts/harness_ticker.gd` (built in code) shows `TESTS RUNNING - JOIN IS LOCKED / Scenario: X / Tests done: X / Y` in the JOIN spot of the lobby and a one-line version under the arena top bar; JOIN MATCH, LOCK IN and JOIN NEXT MATCH hide while locked. Harness: schema entries for the two packets, `Bot.join(bot=None)`, new scenario `harness_gate` (19 total). Harness 19/19. See [[v0.0.23 - Harness Gate and Spectator Overlay]].
- **v0.0.22 "Observability, Harness Telemetry and Bot Arena":** `tools/watch_server.py` is a deck of panels that fold when empty. The harness writes `harness_state.json` once a second (run state, checklist, running scenario, step, findings, warnings, fleet bot cards); the deck shows it with a progress bar and ✔ / ✖ / ▲ marks. New result level `MINOR` (`ctx.warn`): passed, but the server logged an `ERROR` / traceback or a Godot client printed a `WARNING:`. The server counts kills and deaths per seat and keeps a kill timeline with round marks (`match.timeline` in `status.json`, reset when a match starts); the deck draws a `kills/5s` bar chart and one lane per fighter. Every `ping` carries `rtt`, so the deck shows a ping and a health word per seat. Bots say who they are (`"bot"` in `request_join`, `bot_status` card every 5 s from `bot_brain.gd`, never relayed); the deck's bot arena has slots for future brains (`state`, `target`, `combat`, `learning`). `[STATS]` says `players=4 (2 bots)` (later-list item 5 done). Fixes: "1 life left", `_as_int` on `player_died` fields (no more fuzz traceback). Harness 18/18. See [[v0.0.22 - Observability, Harness Telemetry and Bot Arena]].
- **v0.0.21 "Server dashboard" (Phase 3c step 0):** every server log line starts with a tag (`[JOIN]`, `[LEAVE]`, `[CONN]`, `[NAME]`, `[LOCK]`, `[MATCH]`, `[ROUND]`, `[KILL]`, `[NET]`, `[STATS]`); the packet dumps (`[MSG]`, `[SEND]`) are debug level now (`debug.log` only). Colours only on a real terminal; the `HH:MM:SS | LEVEL | message` file format and the banner did not change. The server writes `status.json` once a second (temp file + rename, standard library only, in `.gitignore`): match state, seats, traffic rates, last 40 events. `tools/watch_server.py` draws it as a live dashboard (`rich` is in the venv; `--plain`, `--once`, `--events N`). Harness 18/18. No game code changed. See [[v0.0.21 - Server dashboard]].
- **v0.0.20 "Bubble faces and steady spins":** the v0.0.19 playtest passed (name tags, CHANGE NAME, spawn spread all good). Two fixes: (1) inside both spawn bubbles the fighter looked like a statue, because the bubble code returned before the aim block. The block is now `_update_aim` in `player.gd` and both bubble branches call it, so the aim line and the face follow the mouse or stick, on every screen. (2) A round could start while the arena was still turning (2.5 s spin, 2.6 s round gap). `_finish_spin_now` in `arena.gd` stops the spin and jumps the platforms to the end, and `_platform_tops` now reads `Global.arena_flips % 2` instead of the live angle. Harness 18/18. See [[v0.0.20 - Bubble faces and steady spins]].
- **v0.0.19 "Who is who":** every fighter wears its name above its head (pale yellow for you, white for the others). The server owns the names: a taken name gets a number (`Tav`, `Tav-2`, `Tav-3`; letters kept as typed; 12-letter cap kept by trimming). A `CHANGE NAME` button in the lobby opens the name sheet again with `SAVE NAME` / `CANCEL`. A round now starts with everyone standing on their own platform spot: the game reads the platform tops as they are right now (flips counted, off-screen tops skipped) and picks spots far apart (leftmost first, then always the farthest from the picked ones), in slot order, facing the middle, inside a still 1.5 s bubble that pops on the first key press. The sky bubble after a death is unchanged but starts at `y = 40` (was 70). A `🧭 [Spawn]` console line lists the picked spots each round. New harness scenario `dup_names` (18 total). The v0.0.18 playtest confirmed the top bar, arrow kills and turning arrows; the jump bug did not show, the `🪤 [JumpTrap]` line stays. See [[v0.0.19 - Who is who]].
- **v0.0.18 playtest fixes:** fighters come back in a floating bubble in the upper middle air (random x, same on every screen thanks to a seeded random number; the bubble is a shield: hits bounce, arrows fly back; pops after 5 s, on the ground, or on any attack/dash/special; Down drops faster). Arrows that kill fly on and stick; stuck arrows turn with the platforms. Bots lock in again after a match. Top-bar labels start 20 px in, so the crown number and the first letter of the name show. The R key crown wipe is gone. A `🪤 [JumpTrap]` console line prints if a jump press on the floor is ignored (cause of the "cannot jump with W" bug still unknown). See [[v0.0.18 - Playtest Fixes: Bubble Spawn, Arrows, Bots, HUD]].
- **Phase 3b steps 1–4** shipped in v0.0.12–v0.0.15 and the v0.0.17 hotfix. See [[v0.0.15 - Harness Quality Gate & Spectator Count]] and the three pages before it.
- **v0.0.17 hotfix:** with two clients, a puppet that landed and stood still was drawn sinking through the platform (the receiver kept guessing "still falling" once the sender went quiet). Fixed with a "feet on the ground" bit in `sync_pos` (`FLAG_FLOOR`, no vertical guess when it is set) and one repeated packet when a player comes to rest. New `dips=` count in NetStats; the harness fails on any dip. Also: the puppet top-wrap threshold moved from -10 to -193 (a fighter above the top edge was drawn near the floor). See [[v0.0.17 - Hotfix: prevent puppet floor clipping on jump landing]].
- **v0.0.16 got the phone ready for step 5:** the web page now comes from our own `web/shell.html`. On a phone, one tap on the dark sheet fills the screen and turns the phone sideways (Android). An iPad fills the screen but must be turned by hand. An iPhone cannot do either, so it shows a "turn your phone" sign with a home-screen tip. A PC sees none of it. No game or server code changed. See [[v0.0.16 - Mobile fullscreen and landscape lock support]].
- **Writing rule (2026-09-03, reworded in v0.0.23):** speak plainly, keep the specifics. Plain words and short sentences, no filler, but always the exact file names, function names, packet keys, numbers and schemas. State the cause, state the fix, name the file, give the numbers. Rule 7 in `CLAUDE.md`.
- **Harness 19/19** (`tools/chaos_bots.py --restart-each`), report `harness_report_v0.0.23.json` (one report per build since v0.0.15). The `lag` scenario now includes a headless Godot observer with a brain and gates its puppets; `fleet --ai …` gates every brain's puppets (`fleet.puppet-jitter`: mean ≤ 45 px/s at `--jitter-ms` ≤ 60, ≤ 100 above; `fleet.puppet-snaps`: teleports ≤ deaths seen + 5, seam wraps excluded).
- **What Phase 3b delivered:** `puppets=` telemetry in NetStats (`jitter=<mean>/<p95>px/s snaps wraps stall extrap`), `--jitter-ms`, slot-clock ticks, a 16-sample snapshot ring per puppet rendered 100 ms behind the sender with seam-unwrapped deltas and flags at render time, ≤ 200 ms extrapolation past the newest sample, 80 px teleport snap. Numbers (4-bot fleet, 90 s): lerp ≈ 290 / 680 px/s (mean / p95) → ≈ 20 / 90 at 0–120 ± 40 ms, ≈ 77 / 415 at 120 ± 150 ms.
- **Server:** `[STATS]` line now reads `players=N spectators=M sockets=N+M` (was `sockets=` meaning spectators only).
- **Bot brains milestone tagged `v0.0.11`**: six personas, fleet assertions. Test beds: `--scenario fleet --godot 4 --ai chaser,sniper,turtle,rusher --duration 90 --latency-ms 120 --jitter-ms 40` (standard gate), `--jitter-ms 150` (heavy gate), `--godot 2 --ai chaser,sniper --loss 0.2` (gaps + the protocol oracle).
- **Phase 3a done and playtested on phone + PC:** 20 Hz movement with idle suppression, u16 tick in every movement sample, bounded per-client send queues + stall detection in `serve_game.py`.
- Phases 0, 0.5, 1, 2, 3a and the bot brains complete. Full history: [[Changelog]] and `Patch Notes/`.

## Phase 3b step 5: playtest results and the "later" list
The family playtest ran on 2026-09-03 on a PC (Chromium), a Galaxy S25 Ultra (Chrome) and an iPad Pro. Full notes in [[Playtest v0.0.17]]. In short: phone screen helpers all pass; puppet movement, seams, teleports and landing all pass; hits land close to the drawn body (the 100 ms draw delay, Phase 3c territory). Fixed in v0.0.18: bubble spawn, lost arrows, floating arrows, bots not re-locking, hidden crown number, R key wipe, plus a jump trap.

**Later list (not fixed yet, in rough order):**
1. ~~Who is who~~ **shipped in v0.0.19** (name tags, no duplicate names, CHANGE NAME button, platform start spots). Left over on purpose: arrows drawn in the shooter's colour, and sending the quiver count to other screens. Both go into the Phase 3c authoritative loop.
2. **Seam hiding:** a fighter is only ever drawn on one side of the seam, so it can sit almost fully off the edge and shoot. Fix idea: draw a second copy on the other side when near the edge.
3. **Archer kit (with backlog 1 and 6):** arrows only come back from the archer's own stuck arrows; other classes spam free shots. Ideas: slow regen, steal enemy arrows, send the ammo count to other screens (today only the archer sees it).
4. **Fill-with-bots button** in the lobby (the server would have to start headless bot processes).
5. ~~Bot count in the `[STATS]` line~~ **shipped in v0.0.22** (`"bot"` in `request_join`, `players=4 (2 bots)`).
6. **Slope traction:** fighters slide off tilted platforms too easily while the arena turns.
7. **Air shield feel:** shielding in the air stops all sideways speed (written on purpose; user to decide).
8. **Hiding on top of the flipped ground:** when the arena is upside down the old floor sits at the very top edge and a fighter can stand there almost out of view.
9. **The W jump lock:** wait for a `🪤 [JumpTrap]` line from a real game, then fix.

## How to work here
- **Rules:** `CLAUDE.md` in the repo root (plain words everywhere, rule 7; wrap up and ask for `/clear` after every shipped build, phase end or long session, rule 8; no `.tscn` edits; every GDScript change ships via `./bump_build.sh --title "..."`, which compile-checks with autoloads live, bumps `v0.0.x`, exports, cache-busts the pck, generates the patch-notes page; patch notes on every bump). The user approves one phase at a time; report bugs found outside the phase, don't fix them unasked.
- **Verify:** `godot --headless --path . --script tools/check_scripts.gd -- --no-net` (`--check-only` is useless here) then `./venv/bin/python tools/chaos_bots.py --restart-each --json docs/harness_report_<ver>.json`. `--scenario X` for one; `--list` for all 19; `--soak 30` for leaks; `--scenario play --latency-ms 120 --loss 0.05` for fault injection.
- **Serve/play:** `systemctl --user restart towerbrawl` after any `serve_game.py` change (client-only changes need no restart: the server re-reads the version from `global.gd`). Everyone opens `https://192.168.4.21:8443/play` (redirects to the current build; PC on plain http is not a secure context in Chromium). Logs: `server.log` (never rotated, contains days of history: filter by the last "Game version" banner line; every line starts with a tag since v0.0.21), `.harness_logs/godot*.log`. **Live view while playing or testing:** `./venv/bin/python tools/watch_server.py` (reads `status.json` from the server and `harness_state.json` from the harness; `--plain` / `--once`; the deck folds panels it has nothing for).
- **Headless real client:** `godot --headless --path . -- --autojoin --name=X --class=N` (NetStats line every 5 s in its stdout). Add `--ai=<persona> [--ai-seed=N] [--ai-difficulty=0..1] [--latency-ms=N]` for a bot brain (`🧠 [Bot …]` status line every 5 s with action counts, wrap stats and the aim self-check; the persona picks its class unless `--class` is given). Harness: `--scenario fleet --godot 4 --ai chaser,sniper,turtle,griefer` (comma list = one persona per client, no protocol bots at 4), `--godot 2 --ai sniper,rusher` (2 brains + 2 protocol bots), `--ai-difficulty 0.3`, `--latency-ms 120` also reaches the Godot clients. Bot logs land in `.harness_logs/godot<N>.log`.
- **Docs live in `docs/`** (= the vault). Commands and gotchas: [[obsidian_notes]].

## Architecture in one screen
- `serve_game.py`: authoritative lobby/match state (slots, playing/waiting, alive, stocks, scores, rounds, 5-crown match end), relay for everything else. Harness gate (v0.0.23): `_harness_tick` reads `harness_state.json`, `harness_gate` dict, `join_locked` / `harness_status` packets, `_demote_humans`, `_release_seat`. Tagged log lines and a `status.json` picture once a second (`_build_status` / `status_loop`, v0.0.21) for `tools/watch_server.py`; since v0.0.22 it also carries kills, deaths, `rtt_ms` and the bot card per seat, and the match's kill timeline (`match_kd`, `match_timeline`, `player_bots`). One reader thread per socket + one `ClientConn` writer thread with a bounded queue (movement dropped first when backed up; stalled peers dropped after ~5 s of a non-shrinking kernel backlog). TCP keepalive instead of app-level timeouts (hidden tabs keep their seat). Frame cap 4 KB, relay cap 1 KB. Sender stamped server-side. Client token gates slot reclaim; 8 s rejoin grace mid-match; 1.5 s forfeit grace on explicit leave; observer-authoritative deaths deduped per 1.5 s.
- `scripts/global.gd`: the only network code on the client. JSON for events, **binary for movement**: `sync_pos` 11 B `[type, sender, tick u16 (20 Hz slot clock), x*10 s16, y*10 s16, aim 0.1° u16, flags]` (flag bits: facing, dash, shield, bear, egg, feet-on-the-ground), `spawn_projectile` 9 B. Layout mirrored in `serve_game.py BIN_TYPES` and `tools/chaos_bots.py`. Signals out to `arena.gd` / `character_select.gd` (both connect from a table and disconnect in `_exit_tree`). Auto-rejoin after a reconnect, scene correction to the server state, combat state (ammo, tick) persisted for reloads.
- `scripts/player.gd`: local sim + 20 Hz send with idle suppression on a slot-clock tick; remote puppets render from a 16-sample snapshot ring 100 ms behind the sender (`_render_snapshots`: seam-unwrapped, flags at render time, ≤ 200 ms extrapolation past the newest sample, 80 px teleport snap; Phase 3b); per-frame puppet-jitter telemetry into `Global.puppet_sample`; deaths reported by whoever saw the hit.
- `scripts/harness_ticker.gd`: the small test ticker panel (v0.0.23). Listens to `Global.net_harness_status`, hides when `active` is false. Lobby makes a 3-line one, arena a 1-line one.
- `scripts/bot_brain.gd`: AI opponent attached by `arena.gd` to the local fighter under `--ai=<persona>`. Perceives from the scene tree, acts with `Input.action_press/release` (runs before the fighter in the same physics frame via `process_physics_priority = -10`), aims with a synthetic `InputEventMouseMotion` (verified: 0.0° error). 10 Hz decisions through a reaction-delay queue, gaussian aim error, seeded RNG. Navigation: wrap-aware steering, rises beyond one jump use a jump + upward dash at the apex or a drop through the bottom seam, and an airborne bot over an empty column air-steers to its landing platform (`_landing_move`). Shared: inbound-projectile threat test, lead-shot solver, evade by dash. Personas: `wanderer`, `chaser` (knight), `sniper` (mage), `turtle` (knight), `rusher` (rogue), `griefer` (druid: seam drops and hops, powerup camping, dashes into projectiles, shield spam, bear toggles).
- `web/shell.html`: the web page that holds the game. Godot fills in its `$GODOT_*` blanks at export time (`html/custom_html_shell` in `export_presets.cfg`; `bump_build.sh` stops if the exported page lacks the helpers). It adds three phone helpers on top of the game: a "tap to play full screen" sheet (fill the screen, then ask the phone to turn sideways), a corner button to get full screen back, and a style-only "turn your phone sideways" sign.
- `tools/chaos_bots.py`: protocol oracle + 19 scenarios + fuzz + fault injection + soak + `GodotClient` fleet runner + `StatePublisher` (`harness_state.json`, v0.0.22) + `ctx.warn` for MINOR results (parses the `🧠 [Bot` line: `fleet.bot-idle`, `fleet.bot-fall-loop`, `fleet.bot-missing`, `fleet.no-kills`, `fleet.no-round-end` at 150 s+; parses `puppets=`: `puppet_gate` → `fleet.puppet-jitter` / `fleet.puppet-snaps`, also on the `lag` scenario's Godot observer).

## Open backlog / edge cases
1. **Ranger reload mid-match** loses the stuck arrows on the ground (projectiles are not in any snapshot) → 0 ammo, no pickups until the next round. Simple fallback: restore ≥1 arrow on a mid-match rejoin; proper fix with the class-kit revisit (arrows in the join snapshot, or slow regen). See [[Playtest v0.0.8]].
2. ~~`p0_left` InputMap error~~ **fixed in v0.0.11**: the three P1-bind copies in `global.gd` now require `my_player_id >= 2` (id 0 = spectator). Was the intermittent red on the default fleet scenario.
3. Spectating from a second tab of the same browser shares Chromium's renderer main thread with the game tab (input stalls during heavy work in the other tab). Mitigated (inputs released on focus loss); use a second device/profile to spectate.
4. `server.log` / `debug.log` never rotate (untracked; multi-day files). Consider rotation.
5. Godot client `player_configs` still carries a default class per slot; harmless.
6. **Ranger arrow economy:** three arrows per life and pickups only from own stuck arrows; a bot (or a player) that fires off-screen is disarmed for the round. The sniper persona plays mage for this reason. Same root as item 1 (class-kit revisit: arrows in the join snapshot or slow regen).
7. ~~No screen wrap while shielding or in egg form~~ **fixed in v0.0.11** (`_check_screen_wrap()` in both early-return branches of `player.gd`). Found by the griefer persona.
8. ~~Turtle can chain 4 bottom wraps~~ **fixed in v0.0.13** (no air shield over an empty column, as the griefer).
9. **A fighter can get stuck above the top edge** (found by the v0.0.17 dips counter in the 120 ± 150 ms fleet): at x ≈ 181–190 or x ≈ 393, y ≈ -36 to -57, `is_on_floor()` true, 3 slide contacts, pushed up 17 px per tick while moving only sideways. No platform is up there in `arena.tscn`; projectiles and the power-up are not solid bodies. Cause unknown; arena question, not netcode. Puppets now draw it where it is (the top-wrap threshold moved from -10 to -193).
10. **Egg form runs on puppets (still to check; hard to reach):** every weapon skips or reflects a shielding druid, so only a head stomp turns the phoenix into an egg. In the playtest nobody managed it. Also: in `player.gd` `_physics_process`, the `if is_egg:` branch (gravity, `move_and_slide`, `_sync_network_state`) runs before the `if not is_local_player` check, so a puppet drawn as an egg falls with local gravity and sends `sync_pos` as the observer. Not verified in play; check with a druid (griefer persona) in the match.
11. **Arena voids are a mechanic, not a bug** (user decision, v0.0.11): the columns beside the seam (x < 55, x > 585) and the bottom seam are the way to high ground. Bots plan routes through them (`bot_brain.gd` `_steer_to`, `_landing_move`); humans can still fall-loop for a while.


---
tags: [changelog, index]
---
# Changelog

Every build gets its own page in the `Patch Notes/` folder, newest first here. Pages are generated by `./bump_build.sh --title "..."`, which also inserts the link below. Explorer tip: with the folder sorted by file name Z→A the newest version sits on top.

<!-- bump_build.sh inserts new versions directly below this line -->
- [[v0.0.32 - Free-Fall Arena Shift]] — 2026-09-05 · v0.1.0 list, bug build 5 (backlog 9 closed, option A) · harness 23/23 with `--restart-each`, no Minor · `arena.gd` `_set_platforms_solid` zeroes `collision_layer` on every `StaticBody2D` under `Platforms` while the stage turns and restores it when the turn ends (timer or `_finish_spin_now`); fighters fall through the turning stage instead of riding a platform out of the top of the screen; heavy fleet seed 13: 0 `🧗 [TopEdge]` lines, backlog 9 closed, but `fleet.bot-fall-loop` trips on the free fall itself (4 bottom wraps in one 2.5 s turn) = new backlog 23, a design call; new sheet [[Playtest v0.0.32 — Free-Fall Arena Shift]]
- [[v0.0.31 - Top Edge Probe]] — 2026-09-05 · v0.1.0 list, bug build 4 (backlog 9, cause found) · harness 23/23 with `--restart-each`, no Minor · `player.gd` `_probe_top_edge` prints a `🧗 [TopEdge]` line with the collider names when a local fighter touches something above the top edge; `chaos_bots.py` reports the lines per fleet client; three heavy fleet runs show the arena shift carrying a fighter out of the top of the screen on `LedgeRight` / `GroundLeft` (the outer platforms pass 29 to 36 px above the edge mid-turn); no game rule changed, the fix is a design call (A: platforms not solid during the turn)
- [[v0.0.30 - Ranger Rejoin Quiver]] — 2026-09-05 · v0.1.0 list, bug build 3 (backlog 1 and 6) · harness 23/23 with --restart-each, no Minor · `player.gd` `restore_combat_state` ignores a save from another round (the fresh quiver stays) and gives a same-round ranger at least 1 arrow (the stuck arrows on the ground are not in the join snapshot); `🏹 [Quiver]` console line per rejoin; new scenario `ranger_rejoin`; backlog 15 explained (a same-frame attack + special fires two identical arrow packets, `Bot.double_shot_ok`); new sheet [[Playtest Backlog 1 — Ranger Reload Quiver]]
- [[v0.0.29 - Egg-Form Puppet]] — 2026-09-05 · v0.1.0 list, bug build 2 (backlog 10) · harness 22/22 with --restart-each, no Minor · the `if is_egg:` branch in `player.gd` `_physics_process` moved below the `if not is_local_player:` check, so a puppet never runs local gravity or sends `sync_pos` under the observer's seat; the stomp-egg turns out to have no author over the network (backlog 16, a design call); new sheet [[Playtest Backlog 13 — PC Reload Input Probe]]; [[Playtest v0.0.26]] restored
- [[v0.0.28 - Rejoin in the Replay Gap]] — 2026-09-05 · v0.1.0 list, bug build 1 (backlog 13) · harness 22/22 · a seat still held by the server counts as present when the next round starts (`_next_round_later`, no more "only 1 player left" for a drop inside the replay gap), an auto-rejoin that lands in LOBBY leaves the arena (`_ensure_scene_for_state` after `assign_id`), `Global.current_round` follows the server, `keys= focus=` keyboard probe at the end of every `📈 [NetStats]` line plus a canvas focus self-heal (`focus_canvas`, `web/shell.html`), `--reclaim=N` test hook, new scenario `reload_in_gap`; the PC keyboard-after-reload part stays open with the probe
- [[v0.0.27 - Replay Playtest Sheet and Frame Telemetry]] — 2026-09-04 · v0.1.0 list step 1: the replay playtest sheet · harness 21/21 (second run; first run one-off `fleet.client-silent`) · [[Playtest v0.0.26]] test sheet (gap, slow-motion window, phone caption, paused world, odd cases, a numbers table) and a frame-rate field at the end of every `📈 [NetStats]` line (`fps draw=59.8 phys=60.0 worst=21ms hitches=0`, counted in `global.gd`); no game rule changed
- [[v0.0.26 - Play the Tape]] — 2026-09-04 · v0.1.0 groundwork: the VHS replay · harness 21/21 · when a round ends on a kill every screen plays its frozen tape back (`scripts/replay_player.gd`: rewind, 2.5 s at full speed, the kill in slow motion with a flash, the fall, fade to black, about 5.05 s), the server says `"replay": true` in `round_end` and waits 6.5 s (`REPLAY_ROUND_DELAY`; 7.0 s after a won match), ghosts are real fighter and projectile scenes with `is_ghost`, VHS overlay, spectators too, `📼 [Replay]` line and `history_status.replay` card, deck `▶ R3` / `■ R3 ✓ ▶5.1s`, new `replay` scenario and `replay_gate`
- [[v0.0.25 - History Ring and Kill Stamps]] — 2026-09-04 · Phase 3c: the history ring and kill stamps · harness 20/20 · every screen keeps a 360-frame tape of the fight (`scripts/history_ring.gd`, 60 Hz, 5 s before a kill + 1 s after), the server's `player_died` carries the weapon and stamps the tape, the round end records a 1 s tail and freezes it, `history_status` cards land in `status.json` as `seats[].tape`, `[TAPE]` log lines, `Tape` column and inspect text on the deck (key `0`), new `tape` scenario
- [[v0.0.24 - Full Operations Deck and Interactive Combat Timeline]] — 2026-09-04 · before Phase 3c, the deck as a tool · harness 19/19 · keyboard and mouse on the deck (`tools/deck_input.py`), fight strip at 1 s per column with pan, zoom, click-to-inspect, weapon glyphs, seat colours, round bands and end marks, bot arena for every bot kind, height budget, ping and traffic curves, harness ETA, alert line, log filters, `tbdash` alias; server stamps round ends, match end and names on the timeline
- [[v0.0.23 - Harness Gate and Spectator Overlay]] — 2026-09-03 · before Phase 3c, protect test runs · harness 19/19 · the server reads `harness_state.json` and keeps the seats for bots while a run is alive (`join_locked`, humans demoted to spectator), `harness_status` ticker packet, on-screen test ticker in lobby and arena, `"bot": "headless"` for plain headless clients, new `harness_gate` scenario
- [[v0.0.22 - Observability, Harness Telemetry and Bot Arena]] — 2026-09-03 · before Phase 3c, the live operations deck · harness 18/18 · `harness_state.json` + a PASS / FAIL / Minor checklist on the dashboard, kill timeline with round marks, K/D and ping per seat, bot arena with slots for future bot brains, `bot_status` packets, "1 life left", no fuzz traceback
- [[v0.0.21 - Server dashboard]] — 2026-09-03 · Phase 3c step 0 · harness 18/18 · a tag on every server log line, `status.json` once a second, `tools/watch_server.py` live dashboard; no game code changed
- [[v0.0.20 - Bubble faces and steady spins]] — 2026-09-03 · Phase 3b step 5 playtest fixes · harness 18/18 · the fighter turns and aims inside both spawn bubbles, a round start finishes a running arena spin and reads the spots from the server's flip count
- [[v0.0.19 - Who is who]] — 2026-09-03 · Phase 3b step 5 playtest fixes · harness 18/18 · name tags over fighters, no duplicate names (server adds -2), CHANGE NAME button, round start on platform spots with a still bubble, sky bubble higher, `dup_names` scenario
- [[v0.0.18 - Playtest Fixes: Bubble Spawn, Arrows, Bots, HUD]] — 2026-09-03 · Phase 3b step 5 playtest fixes · harness 17/17 · bubble spawn (random air spot, shield bubble), arrows that kill fly on and stick, stuck arrows turn with the arena, bots re-lock after a match, crown number shows, jump trap
- [[v0.0.17 - Hotfix: prevent puppet floor clipping on jump landing]] — 2026-09-03 · Phase 3b hotfix · harness 17/17 · a landed puppet no longer sinks through the platform: "feet on the ground" flag + one "I stopped" packet; `dips=` counter and gate
- [[v0.0.16 - Mobile fullscreen and landscape lock support]] — 2026-09-03 · phone ready for the playtest · harness 17/17 · one tap fills the screen and turns the phone sideways; "turn your phone" sign for the rest; game code untouched
- [[v0.0.15 - Harness Quality Gate & Spectator Count]] — 2026-09-03 · Phase 3b step 4 · harness 17/17 · `puppet_gate` on fleet --ai and a Godot observer in `lag`; `[STATS]` spectators
- [[v0.0.14 - Stall Extrapolation & Teleport Snapping]] — 2026-09-03 · Phase 3b step 3 · fleets PASS, oracle clean · ≤ 200 ms extrapolation, 80 px teleport snap, `extrap=` field; heavy-jitter p95 1100 → 415 px/s
- [[v0.0.13 - Snapshot Interpolation]] — 2026-09-02 · Phase 3b step 2 · fleets PASS · slot-clock ticks, snapshot ring + 100 ms render delay, seam-aware; jitter ≈ 290 → ≈ 20 px/s
- [[v0.0.12 - Puppet Jitter Metric & Send Jitter]] — 2026-09-02 · Phase 3b step 1 · fleet baselines · `puppets=` in NetStats, `--jitter-ms`; lerp baseline jitter ≈ 290 px/s mean, ≈ 680 p95
- [[v0.0.11 - Griefer, Wrap Navigation & Harness Assertions]] — 2026-09-02 · AI bot brains steps 5–7, milestone tagged · harness 17/17 · griefer, seam routes + air-steered landings, fleet bot assertions, p0_left fixed
- [[v0.0.10 - Chaser, Sniper, Turtle & Rusher]] — 2026-09-02 · AI bot brains step 4 · harness 16/17 (p0_left race, pre-existing) · four combat personas, void guard, cape polygon fix
- [[v0.0.9 - Bot Brain Plumbing & Wanderer]] — 2026-09-02 · AI bot brains steps 1–3 · harness 17/17 · `--ai=` flags, bot_brain.gd core, wanderer persona
- [[v0.0.8 - Send Queues & 20 Hz Tick]] — 2026-09-02 · Phase 3a · harness 17/17 · per-client send queues, 20 Hz + idle suppression, tick numbers
- [[v0.0.7 - Touch Input & Rejoin State]] — 2026-09-02 · phone pass · harness 16/17 · no mouse binds on touch, ammo survives a reload
- [[v0.0.6 - Playtest Fixes]] — 2026-09-02 · playtest fixes · harness 16/17 · keepalive, observer deaths, token slots, rejoin grace
- [[v0.0.5 - Parser Hardening & Binary Compression]] — 2026-09-02 · Phase 2 · harness 13/14 · movement packets 189 B → 9 B
- [[v0.0.4 - Event-Driven Cleanup]] — 2026-09-02 · Phase 1 · harness 9/9, Step-2 harness 12/14 · timer pings, signal-only projectiles
- [[v0.0.3 - Match End, Roster & Version Banner]] — 2026-09-02 · cleanup · harness 9/9 · 5-crown match end, no ghost puppets
- [[v0.0.2 - Stability Pass]] — 2026-09-02 · Phase 0.5 · harness 8/8 · packet doubling fixed, TCP_NODELAY, force_die
- [[v0.0.1 - Instrumentation & Build Pipeline]] — 2026-09-02 · Phase 0 · harness 1/8 (baseline) · bump_build.sh, NetStats, harness

## Roadmap
- Phase 3b: snapshot interpolation + puppet-jitter metric (v0.0.12–v0.0.15, hotfix v0.0.17) and the family playtest ([[Playtest v0.0.17]]), done.
- Playtest fix builds: v0.0.18, v0.0.19 "Who is who" and v0.0.20 "Bubble faces and steady spins" shipped. The v0.0.19 playtest passed.
- Phase 3c step 0 (v0.0.21): server log tags, `status.json`, the live dashboard. Done.
- v0.0.22: the dashboard became a live operations deck (harness deck, fight timeline, bot arena, ping and K/D per seat). Done.
- v0.0.23: the harness gate (seats for bots only while a run is alive) and the spectator test ticker. Done.
- v0.0.24: the full operations deck (keys, mouse, interactive fight strip, bot arena for every bot). Done.
- Phase 3c (v0.0.25): the 5-second history ring and the kill-frame stamps. Done.
- v0.0.26: the VHS replay plays the frozen tape back after every round won on a kill (6.5 s gap). Done.
- v0.0.27: the replay playtest sheet ([[Playtest v0.0.26]]) and the fps field in `NetStats`. Done. The playtest was played on 2026-09-04: the replay numbers stay.
- v0.0.28: bug build 1, backlog 13 (a drop or reload inside the replay gap keeps the seat and the match; the PC keyboard part carries a probe). Done.
- v0.0.29: bug build 2, backlog 10 (the egg branch below the puppet check; the stomp-egg needs an author = backlog 16). Done.
- **next** the remaining bug builds, one each (backlog 1 and 6 ranger arrows with the simple fallback, backlog 9 stuck at the top), then the optimisation plan. Replay ideas (a match-end montage of every round's closing kill, a skip key) are listed in the PASSDOWN under v0.1.0 ideas.
- **v0.1.0** tagged when the replay, the netcode bugs and the optimisation plan are all done.

Related: [[PASSDOWN]] (start here in a new session), [[Commands]] (commands, server, harness usage), [[closed]] (closed backlog items), the playtest sheets in `archive/playtests/` ([[Playtest v0.0.5]], [[Playtest v0.0.8]], [[Playtest v0.0.17]], [[Playtest v0.0.26]]).
